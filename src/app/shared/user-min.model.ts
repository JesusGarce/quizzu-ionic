export class UserMin {
    id: string;
    username: string;
    profile: string;
    
    constructor(id, username, profile) {
        this.id = id;
        this.username = username;
        this.profile = profile;
    }
}
